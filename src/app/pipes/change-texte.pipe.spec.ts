import { ChangeTextePipe } from './change-texte.pipe';

describe('ChangeTextePipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeTextePipe();
    expect(pipe).toBeTruthy();
  });
});
